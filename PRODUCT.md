# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

主要用户是 Windows 本地赛事现场的操作员与开发者。他们需要在高频、持续值守的导播环境中快速确认系统状态并完成计时、BP、OBS 推送、素材与日志操作。

## Product Purpose

星澜赛事导播系统围绕 OBS Studio 提供本地、离线可用的赛事控制台。成功意味着导播可以在一个稳定入口内完成关键操作，并在异常时清楚知道当前状态和恢复路径。

## Positioning

系统把七阶段 Ban/Pick、BO3 调度、赛事素材和 OBS WebSocket 串行控制整合为一个仅监听本机回环地址的导播工作台。

## Operating Context

- Windows 本机运行，服务默认监听 `127.0.0.1:3788`。
- 登录后通过单地址控制台切换不同操作面板，OBS Browser Source 使用独立免登录输出页。
- 操作员负责日常导播动作，开发者负责高风险配置与文件写操作。
- 导播可能长时间连续使用，界面需适合快速扫描、键盘操作与窄窗口。

## Capabilities and Constraints

- 核心能力包括计时、BP、赛果与赛程调度、OBS 同步、素材管理、日志和更新日志。
- 角色数据页按 BP 有赛果的最新有效局统计逃生与追捕角色的使用、禁用和胜负表现，并通过 BP 事件流动态更新排名。
- 大列表按批次读取；登录后先预取首批与可预测的后续批次，用户接近列表尾部时自动续载，不使用显式“加载更多”入口。
- 用户公开资料、头像、主页顶置图、展示统计与好友关系按账号持久化，用户可在验证当前密码后修改账号和密码。
- 权限身份、账号状态、到期时间与全量个人数据由开发者通过账号管理维护；管理中心对非开发者完全隐藏并由服务端拒绝访问。
- 个人主页展示在岗时间、账号有效期、历史执行赛事和累计执行对局，用户可选择公开哪些统计，但登录地区固定展示。
- 登录地区由本机地址或受信反向代理提供的 IP 地理头自动更新；数据结构默认按后续公网、多节点同步场景设计。
- OBS 写操作必须经过现有串行队列；音乐场景联动保持休眠。
- 用户可见文案统一来自 `public/assets/data/ui-text.json`。
- 本机数据库、OBS 密码、会话令牌和迁移备份不得进入版本控制或用户可见日志。

## Brand Commitments

- 产品名为“星澜赛事导播系统”。
- 保留现有浅色、克制、工作台式视觉体系以及蓝色主状态语义。
- 新页面继承现有卡片、线性缓动和逐层飞入动效，不替换整站视觉语言。
- 右上角用户状态由独立圆形头像和三行圆角信息面组成，头像是个人主页的固定入口。

## Evidence on Hand

- 当前产品实现位于 `public/control.html`、`public/assets/css/control.css` 和相关原生 JavaScript 文件。
- 品牌标志位于 `public/assets/brand/stella-logo.png`。
- 当前产品上下文以 `v2.2.0` 记忆文件和实际仓库状态为基线。

## Product Principles

- 关键状态先于装饰。
- 高频操作保持直接、稳定和可恢复。
- 个性化不改变权限边界或 OBS 输出可靠性。
- 本机数据默认受到保护。
- 新能力沿用现有导航、文案和动效体系。

## Accessibility & Inclusion

表单控件提供可访问名称、键盘焦点、关联错误信息和状态播报；非必要动画尊重 `prefers-reduced-motion`。
